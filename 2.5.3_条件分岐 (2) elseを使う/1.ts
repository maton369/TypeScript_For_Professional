// userName に値があるかどうかをチェック
if (userName !== "") {
    // 空でなければ、ユーザーを褒めるメッセージを表示
    console.log("ちゃんと名前があってえらい！");
} else {
    // 空であれば、名前の入力を促すメッセージを表示
    console.log("名前を入力してください！");
    // デフォルト名として "名無し" を代入
    userName = "名無し";
}