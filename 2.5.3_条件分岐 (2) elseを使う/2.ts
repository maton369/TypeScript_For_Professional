// num の値に応じて条件分岐を行う
if (num < 0) {
    // num が負の値であれば、このブロックが実行される
    console.log("numは負の数です");
} else if (num === 0) {
    // num が 0 の場合に実行される
    console.log("numは0です");
} else {
    // num が正の数（0 より大きい）であればこのブロックが実行される
    console.log("numは正の数です");
}