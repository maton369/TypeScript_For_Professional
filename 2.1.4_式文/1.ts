// `greeting` と `target` という2つの変数を `+` 演算子で結合し、
// その結果の文字列をコンソールに出力する。
// どちらの変数も `string` 型である必要がある。
// たとえば greeting = "Hello, ", target = "world!" の場合、出力は "Hello, world!" となる。
console.log(greeting + target);