// 1から100までの数を順に処理
for (let i = 1; i <= 100; i++) {
    // i が奇数の場合はこの周の残りの処理をスキップ（continue）
    if (i % 2 === 1) {
        continue;
    }

    // i が偶数の場合のみこの行が実行される
    console.log(i); // 2, 4, 6, ..., 100 が表示される
}