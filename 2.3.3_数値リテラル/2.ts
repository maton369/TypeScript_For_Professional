// `big` は指数表記（exponential notation）で表された数値リテラルである。
// `1e8` は「1 × 10⁸」、すなわち 100000000 を意味する。
const big = 1e8;

// `small` も同様に指数表記で表された小さな数値である。
// `4e-5` は「4 × 10⁻⁵」、すなわち 0.00004 を意味する。
const small = 4e-5;

// `big` および `small` を10進数として標準出力に表示する。
// 出力: 100000000 0.00004
console.log(big, small);