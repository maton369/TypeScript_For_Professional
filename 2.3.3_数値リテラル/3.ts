// `million` は数値リテラル 1_000_000 を代入した定数である。
// アンダースコア `_` は「数値の桁区切り文字」として使用でき、
// 人間にとって読みやすい形で数値を記述するための構文上の工夫である。
// TypeScript では `1_000_000` は `1000000` とまったく同じ意味を持つ。
const million = 1_000_000;

// `million` の値（1000000）をコンソールに出力する。
// 出力: 1000000
console.log(million);