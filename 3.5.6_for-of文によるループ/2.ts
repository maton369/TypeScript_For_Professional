// 数値配列を定義
const arr = [1, 10, 100];

// for...of 構文で配列の各要素を順に取り出す
for (let elm of arr) {
    // 取り出した要素の値を10倍する（elmはコピーなので元の配列は変わらない）
    elm *= 10;

    // 10倍した値を出力
    console.log(elm);
}

// 実行結果:
// 10
// 100
// 1000