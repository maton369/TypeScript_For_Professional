// `no` は論理値（boolean）型の定数として宣言されており、値は `false` である。
// 型注釈 `: boolean` により、真偽値しか代入できないことを明示している。
const no: boolean = false;

// `yes` は同じく boolean 型の定数で、値は `true` である。
const yes: boolean = true;

// `console.log` により、`yes`（true）と `no`（false）を並べて標準出力に表示する。
// 出力: true false
console.log(yes, no);