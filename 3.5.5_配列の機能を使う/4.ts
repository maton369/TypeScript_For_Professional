// ✅ 数値配列を定義
const arr = [1, 10, 100];

// length プロパティは配列の要素数を返す
// 現在の配列は [1, 10, 100] なので要素数は 3
console.log(arr.length); // 3 が表示される

// push(1000) で配列の末尾に 1000 を追加
arr.push(1000);

// 配列は [1, 10, 100, 1000] となり、要素数は 4
console.log(arr.length); // 4 が表示される