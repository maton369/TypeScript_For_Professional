const nums = [1, 2, 3, 4, 5, 6, 7, 8, 9];
// 元の配列nums。1から9までの整数を要素として持つ。

// filterメソッドで3の倍数のみを抽出
// x % 3 === 0 の条件を満たす要素（3, 6, 9）が残る
const arr2 = nums.filter((x) => x % 3 === 0);

// 結果を表示
console.log(arr2); // [3, 6, 9] と表示される