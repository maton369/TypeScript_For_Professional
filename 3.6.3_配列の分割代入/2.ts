// 数値の配列を定義
const arr = [1, 2, 4, 8, 16, 32];

// 配列の要素をインデックスで個別に変数へ代入
const first = arr[0];   // 1つ目の要素（1）を first に代入
const second = arr[1];  // 2つ目の要素（2）を second に代入
const third = arr[2];   // 3つ目の要素（4）を third に代入