// `val1` は null 値を持つ定数である。
// `null` は「値が存在しないこと」を明示的に表す特殊な値。
const val1 = null;

// `val2` は undefined 値を持つ定数である。
// `undefined` は「未定義の値（初期化されていない、または返されていない）」を表す。
const val2 = undefined;

// `val1`（null）と `val2`（undefined）をコンソールに出力する。
// 出力: null undefined
console.log(val1, val2);