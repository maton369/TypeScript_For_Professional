// 定数配列 arr を定義
// 要素は 0, 123, -45600（-456 * 100 の計算結果）
const arr = [0, 123, -456 * 100];

// ✅ これはOK
// const で宣言した配列でも、要素の変更は可能
arr[1] = 5400; // 2番目の要素が 5400 に置き換わる

// ❌ これはエラー
// const 変数そのものに新しい配列を再代入することはできない
// エラー内容: Cannot assign to 'arr' because it is a constant.
arr = [1, 2, 345, 67];