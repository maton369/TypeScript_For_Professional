// プロパティを通常の方法で変数に代入する例

// サンプルのオブジェクト
const obj = { foo: 123, bar: "Hello" };

// obj.foo の値を foo という変数に代入
const foo = obj.foo;

// obj.bar の値を bar という変数に代入
const bar = obj.bar;

console.log(foo); // 123 と表示される
console.log(bar); // "Hello" と表示される