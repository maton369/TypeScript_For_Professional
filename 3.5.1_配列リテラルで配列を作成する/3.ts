// 配列 arr1 を定義
const arr1 = [4, 5, 6];

// スプレッド構文 ...arr1 を使って配列を展開
// arr2 の中で arr1 の要素 [4, 5, 6] を展開し、[1, 2, 3] の後ろに結合
const arr2 = [1, 2, 3, ...arr1];

// 結果として arr2 は [1, 2, 3, 4, 5, 6] となる
console.log(arr2); // [1, 2, 3, 4, 5, 6] が表示される