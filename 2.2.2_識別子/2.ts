// ❌ 構文エラー：`↑` は識別子（変数名）として使用できない文字である。
// 識別子は通常、Unicode のうち "文字", "_", "$" および一部の漢字などに限定されており、
// 数学記号や記号文字（↑など）は使用できない。
const ↑ = 0;

// ❌ 構文エラー：変数名に空白（スペース）を含めることはできない。
// `foo bar` のように空白を挟むと、TypeScript（および JavaScript）は
// `foo` という変数と `bar` という別のトークンとして解釈してしまう。
const foo bar = 123;